apiVersion: v1
kind: Service
metadata:
  labels:
    app: wordpress
    name: wp-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: wordpress
  externalIPs:
  - 35.186.192.108
